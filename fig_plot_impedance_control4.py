#!/usr/bin/env python
# -*- encoding: utf-8 -*-
"""Write target here

Write detailed description here

Write typical usage example here

@Modify Time      @Author    @Version    @Description
------------      -------    --------    -----------
4/26/23 9:57 PM   yinzikang      1.0         None
"""

# if step >= int_index:
#     for i in range(len(link_names)):
#         viewer.add_marker(pos=robot_int_xpos[i],
#                           mat=robot_int_xmat[i],
#                           type=const.GEOM_MESH,
#                           label='',
#                           rgba=(1, 0, 0, 0.5),
#                           dataid=2*(sim.model.geom_name2id(link_names[i])-2))
# if step >= mid_index:
#     for i in range(len(link_names)):
#         viewer.add_marker(pos=robot_mid_xpos[i],
#                           mat=robot_mid_xmat[i],
#                           type=const.GEOM_MESH,
#                           label='',
#                           rgba=(0, 1, 0, 0.5),
#                           dataid=2*(sim.model.geom_name2id(link_names[i])-2))

